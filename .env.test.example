# Optional target for `npm run test:real`. Use only a non-production host and
# a disposable directory; the suite performs remote mutations and cleanup.
OPENCODE_SSH_TEST_ALIAS=test-host
OPENCODE_SSH_TEST_WORKDIR=/tmp/opencode-ssh-test
